import styled from 'styled-components';
import Grid from 'material-ui/Grid';
import Typography from 'material-ui/Typography';

export const InnerGrid = styled(Grid)`
  padding-top: 15px;
`;

export const MarginParagraph = styled(Typography)`
  && {
    margin-top: 15px;
    margin-bottom: 15px;
  }
`;
